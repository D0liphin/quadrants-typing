"""Type-only array-access protocol shared by `NDArray` and `SharedArray`.

The real declaration lives in `_arraylike.pyi`. Every name here is
annotation-only: it exists for the type checker and has no runtime object.
"""
